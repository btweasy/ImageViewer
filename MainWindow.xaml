<Window x:Class="ImageViewerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ImageViewerApp"
        xmlns:uc="clr-namespace:ImageViewerApp.UserControls"
        Title="Image Viewer"
        Height="600"
        Width="800"
        Icon="Assets/logo.ico">
	<!-- 🔹 иконка приложения -->

	<Grid>

		<!-- Основное содержимое -->
		<DockPanel>
			<Button DockPanel.Dock="Top"
                    Click="OpenFolder_Click"
                    Margin="10"
                    Padding="5">
				Wybierz folder
			</Button>

			<ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
				<ItemsControl x:Name="ImageList">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel />
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<uc:ImageThumbnail />
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</DockPanel>

		<!-- Оверлей с полноэкранным просмотром -->
		<Grid x:Name="Overlay"
              Background="#AA000000"
              Visibility="Collapsed">
			<Border Background="White"
                    Padding="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    MaxWidth="800"
                    MaxHeight="600">
				<Grid>
					<Image x:Name="FullImage" Stretch="Uniform" />
					<Button Content="Zamknąć"
                            Width="80"
                            Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Click="CloseOverlay_Click" />
				</Grid>
			</Border>
			<StackPanel VerticalAlignment="Bottom"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Margin="0,20">
				<Button Content="◀ Wstecz"
                        Width="100"
                        Height="25"
                        Click="PrevImage_Click"
                        Margin="10"/>
				<Button Content="Dalej ▶"
                        Width="100"
                        Height="25"
                        Click="NextImage_Click"
                        Margin="10"/>
			</StackPanel>
		</Grid>

	</Grid>
</Window>
